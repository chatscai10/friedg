/**
 * 應用程式配置文件
 */

// API 根路徑
export const API_BASE_URL = process.env.REACT_APP_API_URL || 'https://api.friedg.app/v1';

// 每頁默認數量
export const DEFAULT_PAGE_SIZE = 10;

// 日期格式
export const DATE_FORMAT = 'yyyy-MM-dd';
export const DATE_TIME_FORMAT = 'yyyy-MM-dd HH:mm:ss'; 